#include "List_temp.h"
#include "type/point.h"


/*----------------------------- Private Defines ------------------------------*/
List__define(int, i);
List__define(float, f);
List__define(double, d);
List__define(unsigned char, c);
List__define(unsigned int, u);
List__define(long, l);
List__define(Point, p);
/*--------------------------- Private Defines END ----------------------------*/
